# Fuzzy Rules of car like path tracking controller 
# ===============author:bkxcyu====================

fuzzy_rules:
  speed_rules:
    - "if Smoothness is Smooth and IntegrallErr is Low and OrientationErr is Low then Speed is Fast"
    - "if Smoothness is Normal and IntegrallErr is Low and OrientationErr is Low then Speed is FFast"
    - "if Smoothness is Rough  and IntegrallErr is Low and OrientationErr is Low then Speed is FFFast"

    - "if Smoothness is Smooth and IntegrallErr is Low and OrientationErr is Medium then Speed is Medium"
    - "if Smoothness is Normal and IntegrallErr is Low and OrientationErr is Medium then Speed is Fast"
    - "if Smoothness is Rough  and IntegrallErr is Low and OrientationErr is Medium then Speed is FFast"

    - "if Smoothness is Smooth and IntegrallErr is Low and OrientationErr is High then Speed is Medium"
    - "if Smoothness is Normal and IntegrallErr is Low and OrientationErr is High then Speed is Medium"
    - "if Smoothness is Rough  and IntegrallErr is Low and OrientationErr is High then Speed is Fast"

    - "if Smoothness is Smooth and IntegrallErr is Medium and OrientationErr is Low then Speed is Medium"
    - "if Smoothness is Normal and IntegrallErr is Medium and OrientationErr is Low then Speed is Medium"
    - "if Smoothness is Rough  and IntegrallErr is Medium and OrientationErr is Low then Speed is Medium"

    - "if Smoothness is Smooth and IntegrallErr is Medium and OrientationErr is Medium then Speed is Medium"
    - "if Smoothness is Normal and IntegrallErr is Medium and OrientationErr is Medium then Speed is Medium"
    - "if Smoothness is Rough  and IntegrallErr is Medium and OrientationErr is Medium then Speed is Medium"

    - "if Smoothness is Smooth and IntegrallErr is Medium and OrientationErr is High then Speed is Medium"
    - "if Smoothness is Normal and IntegrallErr is Medium and OrientationErr is High then Speed is Medium"
    - "if Smoothness is Rough  and IntegrallErr is Medium and OrientationErr is High then Speed is Medium"

    - "if Smoothness is Smooth and IntegrallErr is High and OrientationErr is Low then Speed is Low"
    - "if Smoothness is Normal and IntegrallErr is High and OrientationErr is Low then Speed is Medium"
    - "if Smoothness is Rough  and IntegrallErr is High and OrientationErr is Low then Speed is Medium"

    - "if Smoothness is Smooth and IntegrallErr is High and OrientationErr is Medium then Speed is LLow"
    - "if Smoothness is Normal and IntegrallErr is High and OrientationErr is Medium then Speed is Low"
    - "if Smoothness is Rough  and IntegrallErr is High and OrientationErr is Medium then Speed is Medium"

    - "if Smoothness is Smooth and IntegrallErr is High and OrientationErr is High then Speed is LLLow"
    - "if Smoothness is Normal and IntegrallErr is High and OrientationErr is High then Speed is LLow"
    - "if Smoothness is Rough  and IntegrallErr is High and OrientationErr is High then Speed is Low"


  steering_rules:
    - "if Smoothness is Smooth and AngleErr is LLow and OrientationErr_ is LLow then Speed is Z"
    - "if Smoothness is Normal and AngleErr is LLow and OrientationErr_ is LLow then Speed is LL"
    - "if Smoothness is Rough  and AngleErr is LLow and OrientationErr_ is LLow then Speed is LM"

    - "if Smoothness is Smooth and AngleErr is LLow and OrientationErr_ is LMedium then Speed is LL"
    - "if Smoothness is Normal and AngleErr is LLow and OrientationErr_ is LMedium then Speed is LM"
    - "if Smoothness is Rough  and AngleErr is LLow and OrientationErr_ is LMedium then Speed is LM"

    - "if Smoothness is Smooth and AngleErr is LLow and OrientationErr_ is LHigh then Speed is LM"
    - "if Smoothness is Normal and AngleErr is LLow and OrientationErr_ is LHigh then Speed is LM"
    - "if Smoothness is Rough  and AngleErr is LLow and OrientationErr_ is LHigh then Speed is LM"

    - "if Smoothness is Smooth and AngleErr is LMedium and OrientationErr_ is LLow then Speed is LM"
    - "if Smoothness is Normal and AngleErr is LMedium and OrientationErr_ is LLow then Speed is LM"
    - "if Smoothness is Rough  and AngleErr is LMedium and OrientationErr_ is LLow then Speed is LM"

    - "if Smoothness is Smooth and AngleErr is LMedium and OrientationErr_ is LMedium then Speed is LM"
    - "if Smoothness is Normal and AngleErr is LMedium and OrientationErr_ is LMedium then Speed is LM"
    - "if Smoothness is Rough  and AngleErr is LMedium and OrientationErr_ is LMedium then Speed is LM"

    - "if Smoothness is Smooth and AngleErr is LMedium and OrientationErr_ is LHigh then Speed is LM"
    - "if Smoothness is Normal and AngleErr is LMedium and OrientationErr_ is LHigh then Speed is LM"
    - "if Smoothness is Rough  and AngleErr is LMedium and OrientationErr_ is LHigh then Speed is LM"

    - "if Smoothness is Smooth and AngleErr is LHigh and OrientationErr_ is LLow then Speed is LM"
    - "if Smoothness is Normal and AngleErr is LHigh and OrientationErr_ is LLow then Speed is LM"
    - "if Smoothness is Rough  and AngleErr is LHigh and OrientationErr_ is LLow then Speed is LM"

    - "if Smoothness is Smooth and AngleErr is LHigh and OrientationErr_ is LMedium then Speed is LM"
    - "if Smoothness is Normal and AngleErr is LHigh and OrientationErr_ is LMedium then Speed is LM"
    - "if Smoothness is Rough  and AngleErr is LHigh and OrientationErr_ is LMedium then Speed is LH"

    - "if Smoothness is Smooth and AngleErr is LHigh and OrientationErr_ is LHigh then Speed is LM"
    - "if Smoothness is Normal and AngleErr is LHigh and OrientationErr_ is LHigh then Speed is LH"
    - "if Smoothness is Rough  and AngleErr is LHigh and OrientationErr_ is LHigh then Speed is LHH"

    - "if Smoothness is Smooth and AngleErr is RLow and OrientationErr_ is RLow then Speed is Z"
    - "if Smoothness is Normal and AngleErr is RLow and OrientationErr_ is RLow then Speed is RL"
    - "if Smoothness is Rough  and AngleErr is RLow and OrientationErr_ is RLow then Speed is RM"

    - "if Smoothness is Smooth and AngleErr is RLow and OrientationErr_ is RMedium then Speed is RL"
    - "if Smoothness is Normal and AngleErr is RLow and OrientationErr_ is RMedium then Speed is RM"
    - "if Smoothness is Rough  and AngleErr is RLow and OrientationErr_ is RMedium then Speed is RM"

    - "if Smoothness is Smooth and AngleErr is RLow and OrientationErr_ is RHigh then Speed is RM"
    - "if Smoothness is Normal and AngleErr is RLow and OrientationErr_ is RHigh then Speed is RM"
    - "if Smoothness is Rough  and AngleErr is RLow and OrientationErr_ is RHigh then Speed is RM"

    - "if Smoothness is Smooth and AngleErr is RMedium and OrientationErr_ is RLow then Speed is RM"
    - "if Smoothness is Normal and AngleErr is RMedium and OrientationErr_ is RLow then Speed is RM"
    - "if Smoothness is Rough  and AngleErr is RMedium and OrientationErr_ is RLow then Speed is RM"

    - "if Smoothness is Smooth and AngleErr is RMedium and OrientationErr_ is RMedium then Speed is RM"
    - "if Smoothness is Normal and AngleErr is RMedium and OrientationErr_ is RMedium then Speed is RM"
    - "if Smoothness is Rough  and AngleErr is RMedium and OrientationErr_ is RMedium then Speed is RM"

    - "if Smoothness is Smooth and AngleErr is RMedium and OrientationErr_ is RHigh then Speed is RM"
    - "if Smoothness is Normal and AngleErr is RMedium and OrientationErr_ is RHigh then Speed is RM"
    - "if Smoothness is Rough  and AngleErr is RMedium and OrientationErr_ is RHigh then Speed is RM"

    - "if Smoothness is Smooth and AngleErr is RHigh and OrientationErr_ is RLow then Speed is RM"
    - "if Smoothness is Normal and AngleErr is RHigh and OrientationErr_ is RLow then Speed is RM"
    - "if Smoothness is Rough  and AngleErr is RHigh and OrientationErr_ is RLow then Speed is RM"

    - "if Smoothness is Smooth and AngleErr is RHigh and OrientationErr_ is RMedium then Speed is RM"
    - "if Smoothness is Normal and AngleErr is RHigh and OrientationErr_ is RMedium then Speed is RM"
    - "if Smoothness is Rough  and AngleErr is RHigh and OrientationErr_ is RMedium then Speed is RH"

    - "if Smoothness is Smooth and AngleErr is RHigh and OrientationErr_ is RHigh then Speed is RM"
    - "if Smoothness is Normal and AngleErr is RHigh and OrientationErr_ is RHigh then Speed is RH"
    - "if Smoothness is Rough  and AngleErr is RHigh and OrientationErr_ is RHigh then Speed is RHH"

  Lfw_rules:
    - "if Smoothness is Smooth and currantSpeed is LLLow then Lfw is SShort"
    - "if Smoothness is Normal and currantSpeed is LLLow then Lfw is Short"
    - "if Smoothness is Rough  and currantSpeed is LLLow then Lfw is Medium"

    - "if Smoothness is Smooth and currantSpeed is LLow then Lfw is Short"
    - "if Smoothness is Normal and currantSpeed is LLow then Lfw is Medium"
    - "if Smoothness is Rough  and currantSpeed is LLow then Lfw is Medium"

    - "if Smoothness is Smooth and currantSpeed is Low then Lfw is Medium"
    - "if Smoothness is Normal and currantSpeed is Low then Lfw is Medium"
    - "if Smoothness is Rough  and currantSpeed is Low then Lfw is Medium"
    
    - "if Smoothness is Smooth and currantSpeed is Medium then Lfw is Medium"
    - "if Smoothness is Normal and currantSpeed is Medium then Lfw is Medium"
    - "if Smoothness is Rough  and currantSpeed is Medium then Lfw is Medium"
    
    - "if Smoothness is Smooth and currantSpeed is Fast then Lfw is Medium"
    - "if Smoothness is Normal and currantSpeed is Fast then Lfw is Medium"
    - "if Smoothness is Rough  and currantSpeed is Fast then Lfw is Medium"
    
    - "if Smoothness is Smooth and currantSpeed is FFast then Lfw is Medium"
    - "if Smoothness is Normal and currantSpeed is FFast then Lfw is Medium"
    - "if Smoothness is Rough  and currantSpeed is FFast then Lfw is Long"
    
    - "if Smoothness is Smooth and currantSpeed is FFFast then Lfw is Medium"
    - "if Smoothness is Normal and currantSpeed is FFFast then Lfw is Long"
    - "if Smoothness is Rough  and currantSpeed is FFFast then Lfw is LLong"
    